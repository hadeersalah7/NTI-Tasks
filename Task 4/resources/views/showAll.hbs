{{>header}}
{{>nav}}
<section class="my-5 container">
    <div class="row mb-3 justifiy-content-end">
        <a href="/add" class="btn btn-primary col-2">Add New User</a>
    </div>
    <table class="table table-bordered">
        <thead>


            <tr>
                <th>Account Number</th>
                <th>Name</th>
                <th>Intial Balance</th>
                <th>Remaining Balance</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>


        {{!-- {{#if isEmpty}}
<tr class="alert alert-danger">
                <td colspan="3" class="text-center">No Users Yet</td>
                </tr>
                
                {{else}}
                {{#each customers}} --}}

               
            <tr>
                <td>{{this.id}}</td>
                <td>{{this.name}}</td>
                <td>{{this.intialBalance}}</td>
                <td>{{this.remaining}}</td>
                <td>
                    <a href="/show/{{this.id}}" class="btn btn-primary mx-2">Show Customer</a>
                    <a href="/addOp/{{this.id}}" class="btn btn-success mx-2">Add Operation</a>
                </td>
            </tr>
            {{!-- {{/each}}
        {{/if}} --}}
        </tbody>
    </table>
</section>
{{>footer}}